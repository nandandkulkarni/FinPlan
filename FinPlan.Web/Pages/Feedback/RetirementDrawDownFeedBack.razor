@page "/retirement-drawdown-survey"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        // Redirect to the new, stable implementation
        Nav.NavigateTo("/retirement-drawdown-survey-new", replace: true);
    }
}