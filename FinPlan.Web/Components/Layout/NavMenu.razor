@* ...existing code... *@
<style>
    .nav-debug-window {
        background: rgba(0,0,0,0.85);
        color: #fff;
        padding: 10px 14px;
        border-radius: 8px;
        font-size: 13px;
        margin-bottom: 12px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.2);
        max-height: 180px;
        overflow-y: auto;
    }
</style>

@* Debug window above nav *@
@if (FinPlan.Web.Components.Pages.FamilySavingsPlannerTab.DebugEntries?.Any() == true)
{
    <div class="nav-debug-window">
        <b>Debug Window</b>
        <ul style="padding-left: 1em;">
            @foreach (var entry in FinPlan.Web.Components.Pages.FamilySavingsPlannerTab.DebugEntries.TakeLast(10).Reverse())
            {
                <li>@entry</li>
            }
        </ul>
    </div>
}

@* ...existing code... *@
<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="savings-planner">
                <span class="bi bi-calculator" aria-hidden="true"></span> Wealth Journey
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="retirement-spending" title="Financial Independence, Retire Early">
                <span class="bi bi-wallet2" aria-hidden="true"></span> FIRE Planner
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="tabs">
                <span class="bi bi-table" aria-hidden="true"></span> Data Tabs
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="family-savings">
                <span class="bi bi-people" aria-hidden="true"></span> Family Savings
            </NavLink>
        </div>
    </nav>
</div>
@* ...existing code... *@
